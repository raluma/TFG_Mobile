export type Item = {
    name: string;
};

export type Post = {
    id: string;
    name: string;
    tag: string;
    description: string;
    time: string;
    height: number;
    day: string;
    userId: string;
};